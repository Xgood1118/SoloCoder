用 Java + Spring Boot 写一个缓存代理中间件，支持 Caffeine 作为一级缓存、Redis 作为二级缓存。

功能：
1. 两级缓存：优先查本地，本地miss则查分布式，命中后回填本地缓存
2. 缓存粒度可控：精确到方法+参数组合，支持注解指定缓存Key，支持 spel 表达式
3. 更新策略：主动推送（CacheEvictEvent）和被动过期（TTL），分布式缓存通过 Redis Pub/Sub 同步失效
4. 穿透保护：连续 3 次 miss 生成布隆过滤器，记录该 key 的值不存在，防止缓存击穿