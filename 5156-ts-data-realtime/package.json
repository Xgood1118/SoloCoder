{
  "name": "realtime-data-monitor",
  "version": "1.0.0",
  "description": "Real-time data monitoring platform with WebSocket, sliding window aggregation, and alerting",
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "build": "npm run build --workspace=server && npm run build --workspace=client",
    "start": "cd server && npm start"
  }
}
