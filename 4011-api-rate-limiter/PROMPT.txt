用 Go + Gin 写一个 API 限流中间件，支持令牌桶和滑动窗口两种算法。

功能：
1. 每个接口独立限流，支持按用户ID和IP两种维度，用户优先级高于IP，同一用户共享同一个令牌桶
2. 限流触发时返回 429 + Retry-After 头，超限请求进入排队队列（最多30秒），队列满则直接拒绝
3. 支持动态调整限流参数（QPS阈值、桶大小），热生效无需重启。限流配置存 Redis，TTL 为 2 小时