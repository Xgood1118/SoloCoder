server:
  port: 8080
  mode: debug

database:
  host: localhost
  port: 5432
  user: postgres
  password: postgres
  dbname: purchase_workflow
  sslmode: disable
  timezone: Asia/Shanghai

jwt:
  secret: purchase-workflow-secret-key
  expires_hours: 24

workflow:
  timeout_check_interval: 300
