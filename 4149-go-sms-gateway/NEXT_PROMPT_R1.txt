跑了一下，编译能过但服务起不来，因为没有写HTTP服务器，所有接口都暴露不了，测试根本没法做。单元测试有两个直接崩了（logger里fmt.Sprintf用了%d但是传进去的是字符串，直接panic），还有一个身份证号脱敏的边界情况没处理好。日志格式那个SEND_LOG格式化的时候把状态和耗时顺序写反了，敏感词过滤那个正则只能处理阿拉伯数字，遇到中文数字就失效了。赶紧把HTTP服务器加上，不然下一轮没法测。